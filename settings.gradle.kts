/*
 * SofaBuffers Kotlin Multiplatform corelib — build settings.
 *
 * SPDX-License-Identifier: MIT
 */
rootProject.name = "corelib-kotlin-mp"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
